<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Spinner Game - Earn Points</title>
  <style>
    body {
      font-family: "Poppins", sans-serif;
      text-align: center;
      background: linear-gradient(135deg, #4e54c8, #8f94fb);
      color: white;
      margin: 0;
      padding: 0;
    }

    h1 {
      margin-top: 20px;
    }

    .wheel-container {
      margin: 30px auto;
      width: 250px;
      height: 250px;
      border-radius: 50%;
      border: 8px solid #fff;
      position: relative;
      overflow: hidden;
    }

    .wheel {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      border: 6px solid #fff;
      position: absolute;
      top: 0;
      left: 0;
      transform: rotate(0deg);
      transition: transform 4s ease-out;
    }

    .wheel div {
      position: absolute;
      width: 50%;
      height: 50%;
      background: #ffeb3b;
      transform-origin: 100% 100%;
      text-align: right;
      line-height: 120px;
      padding-right: 10px;
      font-weight: bold;
      color: #333;
    }

    .wheel div:nth-child(1) { transform: rotate(0deg); background: #ff7675; }
    .wheel div:nth-child(2) { transform: rotate(60deg); background: #74b9ff; }
    .wheel div:nth-child(3) { transform: rotate(120deg); background: #55efc4; }
    .wheel div:nth-child(4) { transform: rotate(180deg); background: #ffeaa7; }
    .wheel div:nth-child(5) { transform: rotate(240deg); background: #fd79a8; }
    .wheel div:nth-child(6) { transform: rotate(300deg); background: #a29bfe; }

    .pointer {
      width: 0;
      height: 0;
      border-left: 15px solid transparent;
      border-right: 15px solid transparent;
      border-bottom: 25px solid #fff;
      margin: 0 auto;
    }

    button {
      background: #00b894;
      color: white;
      border: none;
      padding: 12px 20px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      margin-top: 15px;
    }

    button:disabled {
      background: #636e72;
    }

    #points {
      font-size: 22px;
      font-weight: bold;
      margin-top: 10px;
    }

    .withdraw {
      background: rgba(255, 255, 255, 0.1);
      padding: 20px;
      border-radius: 10px;
      width: 90%;
      max-width: 350px;
      margin: 20px auto;
    }

    input {
      padding: 8px;
      border-radius: 6px;
      border: none;
      width: 80%;
      margin-top: 8px;
    }

    .ads {
      margin: 20px auto;
      background: rgba(255,255,255,0.2);
      width: 300px;
      height: 100px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #ddd;
    }
  </style>
</head>
<body>

  <h1>🎡 Spinner Game</h1>
  <div class="pointer"></div>
  <div class="wheel-container">
    <div class="wheel" id="wheel">
      <div>50</div>
      <div>100</div>
      <div>150</div>
      <div>200</div>
      <div>0</div>
      <div>500</div>
    </div>
  </div>

  <button id="spinBtn">Spin Now</button>
  <p id="points">Total Points: 0</p>

  <div class="ads">🪧 Ad Loading...</div>

  <div class="withdraw">
    <h3>💳 Withdraw Section</h3>
    <input type="text" id="phone" placeholder="Enter your mobile number"><br>
    <button id="rechargeBtn" disabled>Recharge 30৳ (30,000 Points)</button><br><br>
    <button id="withdrawBtn" disabled>Withdraw 50৳ (50,000 Points)</button>
    <p id="msg"></p>
  </div>

  <script>
    const wheel = document.getElementById("wheel");
    const spinBtn = document.getElementById("spinBtn");
    const pointsDisplay = document.getElementById("points");
    const rechargeBtn = document.getElementById("rechargeBtn");
    const withdrawBtn = document.getElementById("withdrawBtn");
    const msg = document.getElementById("msg");
    let totalPoints = localStorage.getItem("points") ? parseInt(localStorage.getItem("points")) : 0;

    pointsDisplay.textContent = "Total Points: " + totalPoints;

    spinBtn.addEventListener("click", () => {
      spinBtn.disabled = true;
      const deg = Math.floor(2000 + Math.random() * 2000);
      wheel.style.transform = `rotate(${deg}deg)`;

      setTimeout(() => {
        const section = [50, 100, 150, 200, 0, 500];
        const earned = section[Math.floor(Math.random() * section.length)];
        totalPoints += earned;
        localStorage.setItem("points", totalPoints);
        pointsDisplay.textContent = "Total Points: " + totalPoints;
        alert("You earned " + earned + " points!");
        spinBtn.disabled = false;
        checkWithdrawAvailability();
      }, 4000);
    });

    function checkWithdrawAvailability() {
      rechargeBtn.disabled = totalPoints < 30000;
      withdrawBtn.disabled = totalPoints < 50000;
    }

    rechargeBtn.addEventListener("click", () => {
      const phone = document.getElementById("phone").value;
      if (!phone) return alert("Enter your mobile number!");
      msg.textContent = "📱 Recharge request sent for " + phone;
    });

    withdrawBtn.addEventListener("click", () => {
      const phone = document.getElementById("phone").value;
      if (!phone) return alert("Enter your mobile number!");
      msg.textContent = "💸 Withdraw request sent for " + phone;
    });

    checkWithdrawAvailability();
  </script>

</body>
</html>
